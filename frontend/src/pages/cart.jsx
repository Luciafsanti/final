import styled from "styled-components";
import CartProducts from "../components/cart/cart-products";

const Cart = () => {
  return (
    <CartContainer>
      <CartProducts />
    </CartContainer>
  );
};

export default Cart;
